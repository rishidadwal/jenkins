pipeline {
  agent any 

    stages {

    stage('Git-Clone') {
      steps {
        git url: 'git@github.com:rishidadwal/studentapp-ui-master.git'
  
      }
    }

    stage('Code-Quality') {
      steps {
        sh 'mvn clean compile'
      }
    }

    stage ('sonarqube'){
        steps{
            sh 'mvn sonar:sonar   -Dsonar.projectKey=studentapp-ci  -Dsonar.host.url=http://3.87.221.130:9000   -Dsonar.login=99c7f1cd3c2a6d19cda7a0ba5b88507f7f895c62'
        }
    }
  

  }
}
